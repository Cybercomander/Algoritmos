#include <stdio.h>

/*
//  Este programa debe permitir ingresar n cantidad de números que el usuario 
//  decida aleatoriamente, y el programa deberá ingresarlos a un arreglo con validación.
//  Posteriormente deberá utilizar el algoritmo de ordenamiento bubble sort para ordenar
//  los datos de manera que, cada dígito, esté ordenado de manera ascendente.
//  Seguido de esto, se deberá implementar 2do moddo para permitir visualizar los datos 
//  del arreglo de forma ordenada de manera descendente.
*/


// Función prinicpal del programa
int main(void){
    // Arreglo para ingresar los datos del usuario con límite de 100
    char * data;

    //  Inserción de datos por parte del usuario
    printf("Ingresa los datos del arreglo: \n");
    fflush(stdin);
    getchar();

    return 0;
}

//  Función para ingresar datos del usuario
int* ingresarDatos(char* data){

    //  Determinación de cantidad de caracteres introducidor por el usuario


    // Conversión de caracter (char) a entero (int) e inserción a arreglo
    int i = 0;
    while (data != '/0'){
        i++;
    }

    int array[i];

    while (data != '\0'){
        
    }



    return array;
}